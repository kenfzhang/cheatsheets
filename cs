Create a new web application
$ dotnet new webapp -o MyWebApp --no-https -f net7.0

Create a new Blazor application
$ dotnet new blazorserver -f net7.0

Create a new console application
$ dotnet new console -o <location>

Run application once
$ dotnet run

Run application and refresh when changed
$ dotnet watch
